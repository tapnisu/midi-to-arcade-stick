# midi-to-arcade-stick

Convert midi keyboard inputs to xinput

## OS support

- [x] Windows
- [ ] Linux
- [ ] MacOS

## Configuration

keybinds.cfg in same directory as midi-to-arcade-stick.exe

```
bind 36 up
bind 66 x
bind 68 y
bind 70 rb
bind 65 a
bind 67 b
bind 69 rt
bind 48 start
```
